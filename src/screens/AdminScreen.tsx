import { ScrollView, StyleSheet, Text, View } from 'react-native';
import { colors } from '@/theme/tokens';
export function AdminScreen(){return <ScrollView style={styles.container} contentContainerStyle={styles.content}><Text style={styles.title}>Secure Admin Console</Text>{['Super admins approve clubs, assign roles, moderate content, and review audit logs.','Club admins can edit and announce only for clubs granted in backend policies.','Announcements are scoped to club followers. School-wide messaging requires super admin approval.','Every protected action must pass Supabase RLS and server-side RBAC checks.'].map((t,i)=><View key={i} style={styles.card}><Text style={styles.body}>{t}</Text></View>)}</ScrollView>}
const styles=StyleSheet.create({container:{flex:1,backgroundColor:colors.bg},content:{padding:20},title:{color:colors.text,fontSize:28,fontWeight:'900',marginBottom:16},card:{backgroundColor:colors.surface,borderColor:colors.border,borderWidth:1,borderRadius:18,padding:16,marginBottom:12},body:{color:colors.muted,lineHeight:22}})
