import { ScrollView, StyleSheet, Text, View, Pressable } from 'react-native';
import { clubs, announcements } from '@/data/sampleClubs';
import { colors } from '@/theme/tokens';
export function ClubProfileScreen({route}:any){const club=clubs.find(c=>c.id===route.params.clubId)??clubs[0]; const items=announcements.filter(a=>a.clubId===club.id); return <ScrollView style={styles.container} contentContainerStyle={styles.content}><Text style={styles.kicker}>{club.category}</Text><Text style={styles.title}>{club.name}</Text><Text style={styles.desc}>{club.description}</Text><Pressable style={styles.follow}><Text style={styles.followText}>Follow club notifications</Text></Pressable><Section title="Meeting" body={`${club.meetingDay} at ${club.meetingTime}\n${club.location}`}/><Section title="Advisor" body={club.advisor}/><Section title="Officers" body={club.officers.join('\n')}/><Section title="Contact" body={club.contactEmail}/><Section title="Announcements" body={items.length?items.map(a=>`${a.title}: ${a.body}`).join('\n\n'):'No announcements yet.'}/></ScrollView>}
function Section({title,body}:{title:string;body:string}){return <View style={styles.section}><Text style={styles.sectionTitle}>{title}</Text><Text style={styles.body}>{body}</Text></View>}
const styles=StyleSheet.create({container:{flex:1,backgroundColor:colors.bg},content:{padding:20},kicker:{color:colors.primary,fontWeight:'900',letterSpacing:1.4},title:{color:colors.text,fontSize:32,fontWeight:'900',marginTop:8},desc:{color:colors.muted,fontSize:16,lineHeight:24,marginTop:12},follow:{backgroundColor:colors.primary,padding:15,borderRadius:16,alignItems:'center',marginVertical:20},followText:{color:colors.bg,fontWeight:'900'},section:{backgroundColor:colors.surface,borderColor:colors.border,borderWidth:1,borderRadius:18,padding:16,marginBottom:14},sectionTitle:{color:colors.secondary,fontWeight:'900',fontSize:16,marginBottom:8},body:{color:colors.text,lineHeight:22}})
