/**
 * Layout constants shared between the root shell and the chrome that has to
 * line up with it.
 */

/**
 * On web the app keeps its phone-width column and centres it, so club rows
 * stay a readable measure on a laptop instead of stretching edge to edge.
 */
export const APP_MAX_WIDTH = 720;
