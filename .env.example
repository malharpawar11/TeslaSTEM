# Optional overrides. The committed values in app.json > expo > extra are used by
# default; anything set here wins when running `expo start` locally.
# The anon/publishable key is safe to ship — every table is protected by RLS.
EXPO_PUBLIC_SUPABASE_URL=https://oudyqcyppdgaawitadlr.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-anon-or-publishable-key
EXPO_PUBLIC_ALLOWED_EMAIL_DOMAIN=@lwsd.org
